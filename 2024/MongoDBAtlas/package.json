{
  "name": "mongodbatlas",
  "version": "1.0.0",
  "description": "``` npm init -y npm i typescript @types/node --save-dev npx tsc --init --rootDir src --outDir build ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "realm-web": "^2.0.0"
  }
}
