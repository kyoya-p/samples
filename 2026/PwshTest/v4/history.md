# 実行履歴

## 2026-01-25

### microsoft/tui-test のセットアップ
- GitHubリポジトリ (`microsoft/tui-test`) からクローンし、手動でビルドを実施。
- Node.js v20 環境 (`mise exec node@20`) を使用してビルドおよび実行。
- プロジェクトを ES Modules (`"type": "module"`) に設定。

### 実行テスト 1: `ls`
- **目的**: 新たに起動した `pwsh` で `ls` コマンドを実行し、結果を確認する。
- **結果**: 成功。`README.md` や `package.json` を含むファイル一覧が出力されることを確認。

### 実行テスト 2: `$PID`
- **目的**: 新たに起動した `pwsh` で `$PID` を実行し、プロセスIDを確認する。
- **結果**: 成功。ターミナル上にプロセスID（例: `12552`）が出力されることを確認。
