product: jvm/app

dependencies:
  - ../shared
  - org.jetbrains.kotlinx:kotlinx-coroutines-core:1.10.1
  - org.jline:jline:3.25.0
  - org.fusesource.jansi:jansi:2.4.1
  - net.java.dev.jna:jna:5.14.0
  # GitLive SDK dependencies for JVM might require explicit ktor or coroutines versions if conflicts arise, but Amper should handle it.

settings:
  jvm:
    mainClass: MainKt