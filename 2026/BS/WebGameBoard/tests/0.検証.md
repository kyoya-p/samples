# 検証  
検証は別の gemini-cli インスタンスを起動して実行。  
```
mise x -- gemini -m gemini-3-flash-preview -p "@tests 以下の.mdファイルを実施。評価の際ブラウザ以外(ソースコード等)の情報参照は禁止。テストにはPlaywightを使用。テストで生成するファイルはすべてtestフォルダに格納"
```
- 「コードを確認」「テストモード」での確認は無意味なので禁止
